"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser. So alert() can only be used in browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"     // string
let age = 18            // number
let isLoggedIn = false  // boolean
let state;              // undefined


// number => 2 to power 53(its range)
// bigint  => used for large numbers
// string => ""
// boolean => true/false
// null => standalone value(meaning empty)
// undefined => value is not assigned now
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // so its type is a object **
